# 사주 MBTI — 작업 인계 가이드 (HANDOFF)

> **새 대화 시작 시 이 파일을 첨부하면 맥락이 복원된다.**
> "무엇을 만들었나"가 아니라 "어떻게·왜 작업하는가"의 기준 문서.
> 코드/구조 상세는 README.md 참조.

---

## 0. 한 줄 정체성

생년월일시 + 출생지를 넣으면 **명리(사주) · 점성 · 에니어그램 · MBTI · 직업적성**을 한 번에 분석하는 100% 클라이언트 웹앱. 만세력(ssaju)과 점성(circular-natal-horoscope-js) 둘 다 CDN으로 브라우저에서 직접 계산 — API 키·비용 없음.

---

## 1. 새 대화 인계 멘트 (복붙용)

> "사주 MBTI 엔진 이어서 작업. 첨부 가이드가 기준 문서.
> 추측 말고 GitHub에서 fetch해 실제 코드 확인하고 작업.
> GAS 코드는 GitHub에 없으니 내가 붙여넣어야 읽힘.
> 수정 파일은 다운로드 가능하게, push 전 git fetch+reset 먼저.
> 분석은 3안 제시 후 진행."

---

## 2. 리소스 (verbatim)

- **GitHub**: `https://github.com/checker1016-85/saju-mbti` (main)
- **Git 토큰**: (니노가 별도 보관 — 채팅으로 전달). user: Claude, email: claude@anthropic.com
- **엔진(Vercel)**: `https://saju-mbti-peach.vercel.app/`
- **GAS 배포 ID**: `AKfycbyoLNtcnJXFEGZKI5YPhu2s1-Dv9vz6hMc2byOo2bsyIgnsvMgI_Jd9v-dQN0gf8wfBdg`
- **Drive DB 폴더**: `1FLYP1ghHdbycorY6wT2V4N9ZqNIHIq5O` (일주 DB와 동일 폴더)
- **GAS 속성**: GEMINI_API_KEY, SAZU_API_KEY (저장됨, 현재 미사용)

### 구글시트 DB ID
- 20_일주론 `17WWPiWBCxPp1075eDUW-fut94-4hhXEFvx0IwNzIgx0`
- 30_성별나이 `1D-I2oqGoz8uis4qVhjEIg4Z4ar8lZy7VImo3DUT73Kw`
- 31_외형유형 `1wNL-SO8y8krke9vTBHEYTlmOseqdQMgBVopeLsTUKog`
- 32_직업유형 `1Q_hogo3cm2gxMx2BE6nSfg2j2Spe33QifP61TLDYSfU`
- MBTI `1YP6ogCT9uc5HHE2rEsvvccnS2fP37v9_AXTqtV1wp80`
- 에니어그램(본체+날개 2탭) `1xvEqsPhdIiWSHBKqA9vxg4VZuwi2NGkwa7q4ZtB3Cu0`

---

## 3. 작업 규칙 (니노 지침)

1. 사설(푸념) 없이 명확하게.
2. 명리는 자평진전 정설 + 현대 변환 해석.
3. 분석은 3안 제시 후 선택.
4. 파일명 타임스탬프 `_YYYYMMDD`.
5. Excel: 세로 가운데 정렬 + 넉넉한 열너비/행높이.
6. **GitHub 수정 전 항상** `git fetch origin main && git reset --hard origin/main` 후 재적용 (니노가 웹에서 직접 업로드해 diverge 잦음).
7. 수정 파일은 present_files로 다운로드 제공.
8. Claude는 코드 생성, GitHub push·GAS 배포는 니노가 직접. (단 이 세션에선 Claude가 토큰으로 push 수행 중)

---

## 4. 설계 철학 (불변)

| 계층 | 역할 |
|---|---|
| ssaju CDN | 만세력 계산 (사주4주·십성·격국·강약·12운성·신살·공망·대운) |
| circular-natal-horoscope-js CDN | 점성 계산 (상승궁·행성·하우스·애스펙트) |
| 구글시트 DB | 순수 사전 — 성격·외형·직업·MBTI/에니어그램 설명 |
| JS (프론트) | 두뇌 — 십성→스탯/MBTI/에니어그램, 조합·렌더 |
| GAS | 브릿지 — 시트 읽기, Gemini 호출 |

→ 계산은 CDN, 사전은 시트, 로직은 JS. 절대 섞지 않는다.

---

## 5. 중요 기술 메모 (실수 방지)

- **점성 month는 0-index** → `new Origin({month: month-1})` 필수.
- **공망**은 `r.gongmang.branchesKo`(배열). `r.gongmang` 직접 아님.
- **지지관계**는 `r.branchRelations[타입]={기둥:설명}` 객체 구조.
- **대운**은 `r.daeun.list[]` (startAge/endAge/ganzhi/stemTenGod).
- ssaju import: `https://cdn.jsdelivr.net/npm/ssaju@0.2.0/dist/index.mjs` → `window._ssaju`.
- 점성 import: 동적 `import('https://cdn.jsdelivr.net/npm/circular-natal-horoscope-js@1.1.0/+esm')`.
- **Claude bash 환경에선 jsdelivr CDN 403** (차단). 구조 검증은 `npm install` 로컬로. 브라우저에선 정상.
- DB 설명이 엔진에 안 나오면 → **GAS 재배포 안 한 것**. Driveservice.gs에 시트ID는 반영됨, 재배포는 니노 몫.
- 시각화 4종 200px 통일. 프레임 고정높이 480px. 입력폼 위경도/시간 탭은 고정높이라 전환해도 안 움직임.

---

## 6. 레이아웃 (현재 v11.4)

3단락 세로 (max-width 880px 중앙):
1. 생년월일 입력 (2칼럼 가로: 좌 생년~시간 / 우 성별~조회)
2. 성격 유형 분석 (명리→점성→에니어그램→MBTI→종합, 각 고정프레임 480px, 시각화 우측·키워드 좌측·좌하단 스크롤 메타)
3. 성향 프로필 (스탯5 + 레이더6종 3×2 + 직업)

---

## 7. 추천 우선순위 기준

MBTI·에니어그램·직업 모두 **사주 십성과 가장 가까운 유형이 1순위 추천**. 사용자가 모달(팝업)로 직접 변경 가능 — 변경 시 해당 프레임+종합프로필 실시간 갱신.

---

## 8. 남은 과제

1. MBTI A/T 판정 정교화
2. 점성 애스펙트 해석 텍스트
3. 구글시트 30/31/32 데이터 UI 연결
4. Gemini 코멘트 연동
5. 유형별 공유 카드 이미지
6. 모바일 최적화

---

*기준일: 2026-06-12 / v11.4. 상세는 README.md.*
