# 사주 MBTI — 작업 방식 가이드 (인계용)

> 새 대화에서 이 파일을 첨부하고 시작하면 작업 맥락과 규칙이 복원된다.
> 코드/배포 상세는 `README.md` 참조. 이 문서는 "어떻게 작업하나"가 기준.

---

## 0. 한 줄 정체성

생년월일시 + 출생지를 넣으면 **명리(사주) + 점성 + 에니어그램 + MBTI + 직업적성 + 기질**을 통합 분석하는 클라이언트 엔진.
만세력은 ssaju, 점성은 circular-natal-horoscope-js, 둘 다 CDN 클라이언트 계산 (서버/API 불필요).

---

## 1. 설계 철학 (불변 원칙)

| 계층 | 역할 |
|---|---|
| **ssaju CDN** | 만세력 — 사주4주·십성·격국·강약·12운성·신살·공망·대운 |
| **circular-natal-horoscope-js CDN** | 점성 — ASC·태양·달·MC·행성·하우스·애스펙트 |
| **구글시트 DB** | 순수 사전 — 성격·외형·직업 키워드. 로직 없음 |
| **JS (프론트)** | 두뇌 — 십성→스탯/MBTI/에니어그램 계산, 조합, 렌더링 |
| **GAS (백엔드)** | 브릿지 — 구글시트 읽기, Gemini API 호출 |

→ 시트 수정 → GAS가 읽음 → 프론트 반영. **시트와 로직은 절대 섞지 않는다.**

---

## 2. 도구 분담

| 도구 | 역할 |
|---|---|
| **Claude** | 기획·문서·코드 파일 생성·가이드 (이 세션은 토큰으로 직접 push) |
| **GitHub** | 프론트 코드 관리 `checker1016-85/saju-mbti` |
| **GAS** | 백엔드 (구글시트 읽기 + Gemini) — GitHub에 없음, 채팅에 붙여넣어야 읽힘 |
| **Vercel** | 자동 배포 (GitHub 연동, saju-mbti-peach.vercel.app) |

---

## 3. 작업 규칙 (니노 지침)

1. **사설(푸념) 없이** 명확한 내용만.
2. **명리는 자평진전 정설 기반** + 현대식 해석 추가.
3. **분석은 3안 제시 후 선택** → 최종 (단순 UI 수정은 바로 진행).
4. 문서/시트 파일명에 **타임스탬프 `_YYYYMMDD_HHMM`**.
5. Excel: 세로 가운데 정렬 + 넉넉한 열너비/행높이.
6. 수정 시 GitHub에서 fetch해서 읽고 작업, 다운로드 가능하게 제공.

---

## 4. ★ Git 작업 패턴 (필수 — 반복 충돌 방지)

니노가 웹 UI로 파일을 직접 업로드하는 경우가 잦아 **원격이 자주 diverge**한다.
Claude가 push하기 전 항상:

```bash
git fetch origin main
git reset --hard origin/main   # 원격 최신으로 맞춤
# (내 변경분이 덮였으면) /tmp에 백업해뒀던 내 파일을 다시 복사
# 예: cp /tmp/my_radar.js js/radar.js
node -c js/app.js              # 문법 체크
git add -A && git commit -m "..." && git push origin main
```

- push가 거부되면(non-fast-forward): 내 파일 `/tmp` 백업 → `reset --hard origin/main` → 재복사 → 재커밋 → push.
- `git pull --rebase`는 이 패턴에서 충돌나니 쓰지 말 것.
- **토큰**: `https://[token]@github.com/checker1016-85/saju-mbti.git` (README에 보관)

---

## 5. ★ SVG 시각화 작업 교훈 (반복 실수 방지)

- **도넛 차트**: 닫힌 부채꼴 `path`를 `fill`+`opacity`로 채우고, sweep-flag 정확히(`0 1`/`0 0`). stroke-호 방식은 칸 안쪽이 비어 보임 → 금지.
- 점성 도넛은 **명리 오행 도넛(sajuVizSVG)과 완전 동일 구조**로 (rOut=size/2-10, rIn=size/2-38, opacity 0.88). 칸 수만 5 vs 12.
- 별자리 심볼은 이모지(♈) + `fill=#fff` + `font-family=serif` + 텍스트변환셀렉터 `\uFE0E`로 흰색 렌더.
- 마커(ASC/태양/달/MC)는 **도넛 안쪽**(markerR=rIn-16), 같은 별자리 겹치면 반지름 따라 안쪽 16px씩 들임, 점선 연결.
- **푸시 전 반드시 cairosvg로 렌더 검증** (`pip install cairosvg --break-system-packages`).
- 고정 높이 영역은 텍스트 들어가도 안 흔들리게 px 고정.

---

## 6. ssaju 데이터 구조 (검증됨)

```
r.pillars.{year,month,day,hour}        = "戊午" 등
r.pillarDetails.[hour|day|month|year].stem/.branch
r.tenGods.[기둥].stem/.branch          일간은 stem='(일간)'
r.stemRelations = []                   천간 합/충 있을 때만 [{type,desc:'辛丙 합 → 수',stems}]
r.branchRelations.[육합|삼합|반합|방합|충|형|파|해|원진|귀문] = {기둥:설명}
r.gongmang.{branches:['寅','卯'], branchesKo:['인','묘']}   일주 기준만!
r.daeun.list[] {startAge,endAge,ganzhi,stemTenGod}, r.daeun.startAge
r.stages12.bong.[기둥]                 12운성
r.sals.[기둥].{twelveSal, specialSals[]}
r.advanced.{geukguk, yongsin, dayStrength:{strength:'strong'|'weak',score}}
r.currentAge
```

- **gender: '남'/'여'** (male 아님), calendar: 'solar'/'lunar'
- 년주 공망 직접계산: `s=천간idx, b=지지idx; [BR[(b+10-s)%12], BR[(b+11-s)%12]]`
- 십성 음양: 일간 음양 기준. 戊(양토)→비견 양토/겁재 음토/식신 양금... (편/비견/식신류=일간과 같은 음양, 정/겁재/상관류=반대)

---

## 7. 현재 UI 상태 (v13.8)

### 명리 프레임 구조
```
[헤더 uf-head-split] kw-main(키워드) + 오행도넛
[원국 고정 wongu-fixed, 160px] 좌 wongu-full-left(4주카드 50%) / 우 wongu-full-right(정보 50%, 내부스크롤)
   우측 순서: 대운 → 십성분포(그룹+10종 음양오행) → 십이신살 → 십이운성 → 천간관계 → 지지관계 → 공망
[해석 uf-body-scroll, 480px] 본능일주 / 사회월주
```
- 원국 카드: 한자 위 / 한글 아래 세로. 공망 아이콘 = 옅은 붉은 ● (.gm-dot #e08a8a)
- 천간관계 = 합/충(hl 칩) + 각 기둥 천간 십성 칩
- 공망 = 년주/일주 둘 다 (gm-label 라벨 + 한자(한글) 칩)

### 4프레임 공통: uf-head-split(헤더) + uf-body-scroll(해석)
### 점성: 명리와 동일 도넛, ASC/태양/달/MC 안쪽 마커 + 점선
### MBTI: 4축 둥근박스(opacity 0.2) 양쪽게이지 + A/T 게이지(박스밖)
### 에니어: 9각, 센터 본능/가슴/사고 바깥테두리 pill
### 종합 프로필: 성향 프로필 최상단에만 (맨아래 중복 삭제됨). 4칩(사주/점성/에니어/MBTI) — 점성 칩은 SIGN_KEYWORD 사용, calcAstro가 renderSummary보다 먼저 실행돼야 표시됨

---

## 8. 새 대화 인계 멘트 (복붙용)

> "사주 MBTI 엔진 이어서 작업. 첨부 가이드가 기준 문서.
> 추측 말고 GitHub에서 fetch해 파일 확인, GAS 코드는 내가 붙여넣어야 읽힘.
> Claude는 코드 생성+토큰 push, 배포(Vercel)는 자동. SVG는 푸시 전 cairosvg 렌더 검증.
> git reset --hard origin/main 패턴 지켜서 원격 충돌 방지."

---

## 9. 남은 과제

1. MBTI A/T 판정 정교화 (현재 4축 우세강도 평균)
2. 점성 애스펙트 해석 텍스트
3. 구글시트 30(성별나이)/31(외형)/32(직업) 데이터 UI 본격 연결
4. Gemini 코멘트 연동 (GAS generateComment → 프론트)
5. 유형별 공유 카드 이미지
6. 모바일 반응형 최적화

---

*기준일: 2026-06-16 / v13.8 기준. 배포·계산 상세는 README.md 참조.*
