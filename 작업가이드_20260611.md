# 사주 MBTI — 작업 방식 가이드 (인계용)

> 새 대화에서 이 파일을 첨부하고 시작하면 작업 맥락과 규칙이 복원된다.
> 코드/배포 상세는 `README.md` 참조.

---

## 0. 한 줄 정체성

생년월일을 넣으면 사주 원국의 십성 패턴으로 **MBTI + 에니어그램 + 직업 적성 + 기질 성향**을 분석하는 엔진.
MBTI는 16종이지만, 사주 조합은 60갑자 × 십성 × 성별 = 수천 가지 미세 분기.

---

## 1. 설계 철학 (불변 원칙)

| 계층 | 역할 |
|---|---|
| **ssaju CDN** | 만세력 계산 엔진 — 사주4주·십성·격국·강약·12운성·신살 |
| **구글시트 DB** | 순수 사전 — 성격·외형·직업 키워드. 로직 없음 |
| **JS (프론트)** | 두뇌 — 십성→스탯/MBTI/에니어그램 계산, 조합, 렌더링 |
| **GAS (백엔드)** | 브릿지 — 구글시트 읽기, Gemini API 호출 |

→ 시트 수정 → GAS가 읽음 → 프론트에 반영. **시트와 로직은 절대 섞지 않는다.**

---

## 2. 도구 분담

| 도구 | 역할 |
|---|---|
| **Claude** | 기획·문서·코드 파일 생성·가이드 |
| **Google AI Studio (Gemini)** | 엔진 로직·테스트 (무료 토큰) |
| **GitHub** | 프론트엔드 코드 관리 (`checker1016-85/saju-mbti`) |
| **GAS** | 백엔드 (구글시트 읽기 + Gemini 호출) |
| **Vercel** | 자동 배포 (GitHub 연동) |

- **GAS 코드는 GitHub에 없다** → 수정하려면 채팅에 붙여넣어야 Claude가 읽는다.
- Claude는 **코드 파일만 생성**. GitHub 푸시·GAS 배포는 니노가 직접.

---

## 3. 작업 규칙 (니노 지침)

1. **사설(푸념) 없이** 명확한 내용만.
2. **명리는 자평진전 정설 기반** + 현대식 해석 추가.
3. **분석은 3안 제시 후 선택** → 최종 분석.
4. 문서/시트 파일명에 **타임스탬프 `_YYYYMMDD_HHMM`** 포함.
5. Excel 수정 시: ① 세로 가운데 정렬 ② 넉넉한 열너비·행높이.
6. 수정 시 GitHub에서 fetch해서 읽고 작업.
7. 파일 저장 시 다운로드 할 수 있게 만들 것.

---

## 4. 핵심 엔진 로직

### 4-1. 만세력 (ssaju CDN)

```js
import { calculateSaju } from 'https://cdn.jsdelivr.net/npm/ssaju@0.2.0/dist/index.mjs';
```

제공: 사주4주, 십성, 12운성, 합충형파해, 신살, 격국, 용신, 강약

### 4-2. 십성 → 스탯 5개

일간을 제외한 7개 위치에서 십성 카운트 → 카테고리별 합산 × 14 + 격국/강약 보정

| 스탯 | 십성 |
|------|------|
| 재물력 | 편재 + 정재 |
| 놀기력 | 식신 + 상관 |
| 리더력 | 편관 + 정관 |
| 학습력 | 편인 + 정인 |
| 독립력 | 비견 + 겁재 |

### 4-3. 십성 → MBTI 4축

```
E/I = (식상+재성) vs (인성+비겁)
S/N = (재성+관성) vs (식상+인성)
T/F = (관성+비겁) vs (식상+재성)
J/P = (정관+정재+정인) vs (편관+편재+편인+상관)
```

축 차이 15% 이내 → 양쪽 추천. 사용자가 직접 선택도 가능.

### 4-4. 십성 → 에니어그램

십성 가중치 행렬 × 십성 카운트 → 9유형 점수 → 상위 3개 추천.
메인 선택 시 날개(±1) 자동 표시.

### 4-5. 스탯 → 직업 적성

카테고리별 primary/secondary 스탯 매칭: `score = primary*0.7 + secondary*0.3`

### 4-6. 방사형 레이더 4종

체력·멘탈 / 사회성 / 재능·두뇌 / 재물·야망 (각 5축, SVG 렌더링)

### 4-7. 기질 텍스트

| 소스 | 내용 |
|------|------|
| 일간(10종) | 핵심 기질 (갑~계) |
| 일지(12종) | 내면 에너지 (자~해) |
| 월지(12종) | 사회적 페르소나 |
| 구글시트 30_성별나이 | 천간×성별 상세 (성격/외형/체형/스타일/직업/관계) |

---

## 5. 추천+선택 패턴 (3대 시스템)

직업 · MBTI · 에니어그램 모두 동일 구조:

```
사주 분석 → 추천 (자동, 사주 기반 범위)
          → 선택 (수동, 전체 옵션에서 직접)
          → 선택 변경 시 중앙+우측 실시간 갱신
```

---

## 6. 파일 관리

**GitHub** = 프론트엔드만. GAS 파일은 넣지 않음.

```
GitHub (프론트)              GAS 에디터 (백엔드)
├── css/style.css           ├── appsscript.json
├── js/data.js              ├── Code.gs
├── js/radar.js             ├── Config.gs
├── js/app.js               ├── Driveservice.gs
├── index.html              ├── Geminiservice.gs
├── vercel.json             └── Sajuservice.gs
├── README.md
└── 작업가이드_*.md
```

---

## 7. 구글시트 DB 연동

GAS `getEngineDB()`가 4파일을 읽음 (10번대 제외):

| 파일 | 용도 |
|------|------|
| `20_일주론` | 일주 기초/캐릭터DNA/직업매칭 |
| `30_성별나이` | 천간×성별 성격/외형, 나이대 보정 |
| `31_외형유형` | 외형 유형 분류 |
| `32_직업유형` | 직업 코드/매칭 (240개) |

프론트: `fetch(GAS_URL+'?action=getDB')` → `S.db`에 저장 → 해석 텍스트에 반영.
DB 없으면 하드코딩 텍스트로 폴백.

---

## 8. GAS 스크립트 속성

| 속성 | 용도 |
|------|------|
| GEMINI_API_KEY | Gemini 코멘트 생성 |
| SAZU_API_KEY | 만세력 API (예비) |

---

## 9. UI 규칙

- **라이트 테마** (크림색 배경 #f5f3ee)
- 3칼럼: 좌(입력+원국+해석) / 중(직업+MBTI+에니어그램+스탯) / 우(레이더+종합)
- 시간 입력: 간지/시분 탭 전환, **`.time-box` 고정 42px** (UI 흔들림 방지)
- 나이: select 드롭다운 (만나이/한국나이), 라디오 아님
- 사주 원국: 한자 + 한글 병기 (丙 병(화))
- 직업 선택: 별도 모달 팝업 (15카테고리 130+직업)
- 성별 디폴트: **여**

---

## 10. 새 대화 인계 멘트 (복붙용)

> "사주 MBTI 엔진 이어서 작업. 첨부한 가이드가 기준 문서.
> 추측하지 말고 파일 열어 확인하고, GAS 코드는 내가 붙여넣어야 읽힘.
> Claude는 코드 파일만 생성, 배포는 내가 함. 분석은 3안 제시 후 진행."

---

## 11. 남은 과제

1. 구글시트 30_성별나이 B00 테이블 → 해석 텍스트 본격 반영
2. 에니어그램 날개별 세부 설명
3. Gemini 코멘트 연동 (GAS → 프론트)
4. 외형유형(31) · 직업유형(32) 데이터 UI 연결
5. 유형별 성격 카드 (공유용 이미지)
6. 모바일 반응형 최적화

---

*기준일: 2026-06-11 / 코드·작업 규칙 기준. 배포 상세는 README.md 참조.*
